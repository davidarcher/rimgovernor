services:
  colonies:
    build:
      context: ..
      dockerfile: containers/Dockerfile
      target: directory
    image: rimbot-colonies:local
    command: ["python", "-m", "rimbot", "--colonies", "--host", "0.0.0.0"]
    ports:
      - "127.0.0.1:${RIMBOT_COLONIES_PORT:-8790}:8787"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
