[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rimbot"
version = "0.2.0"
description = "External RimWorld colony controller and dashboard over RimBridgeServer"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115,<1", "uvicorn>=0.30,<1", "httpx>=0.28,<1", "jsonschema>=4.23,<5", "mcp>=1.12,<2", "pydantic>=2,<3", "Pillow>=11,<13"]

[project.optional-dependencies]
video = ["av>=16,<17", "websockets>=14,<16"]
test = ["pytest>=8", "pytest-asyncio>=0.24"]

[project.scripts]
rimbot = "rimbot.__main__:main"

[tool.setuptools.packages.find]
where = ["controller"]

[tool.setuptools.package-data]
rimbot = ["static/*", "static/assets/*", "data/*.json", "data/strategies/*.json"]

[tool.pytest.ini_options]
testpaths = ["controller_tests"]
pythonpath = ["controller"]
asyncio_mode = "auto"
